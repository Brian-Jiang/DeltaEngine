{
  "system": "meta",
  "description": "MCP server introspection — list and describe available queries and commands",
  "queries": {
    "list_operations": {
      "description": "Lightweight index of all systems and their query and command names",
      "params": {}
    },
    "describe_operations": {
      "description": "Full schemas for specific queries and/or commands",
      "params": {
        "targets": {
          "type": "array",
          "required": true,
          "description": "Array of {system,query} or {system,command} objects to describe"
        }
      }
    },
    "capabilities": {
      "description": "Full capability dump — all systems, queries, and commands",
      "params": {
        "system_filter": {
          "type": "string",
          "description": "Limit to a single system",
          "required": false
        }
      }
    }
  },
  "commands": {}
}
