{
  "system": "viewport",
  "description": "Editor viewport camera, rendering settings, and picking",
  "queries": {
    "camera": {
      "description": "Current viewport camera position, rotation (as both quaternion and euler angles), and projection settings",
      "params": {}
    },
    "render_settings": {
      "description": "Current render pipeline settings (resolution, debug overlays, etc.)",
      "params": {}
    },
    "visible_objects": {
      "description": "List of object IDs currently visible in the viewport frustum",
      "params": {
        "include_partial": {
          "type": "bool",
          "default": true,
          "description": "Include partially visible objects",
          "required": false
        }
      }
    }
  },
  "commands": {
    "SetViewportCamera": {
      "description": "Move the viewport fly camera. Position, rotation, and fov are tweened over duration_seconds; near/far planes are not affected.",
      "undoable": false,
      "expects_result": false,
      "expects_result_note": "Returns as soon as the tween starts when duration_seconds > 0. The move is never undoable — the viewport camera is editor-only state.",
      "params": {
        "position": {
          "type": "array",
          "required": false,
          "description": "[x,y,z] world position"
        },
        "rotation": {
          "type": "array",
          "required": false,
          "description": "[x,y,z,w] quaternion or [x,y,z] euler angles in DEGREES; pitch is clamped to +/-89 to match the fly camera"
        },
        "fov": {
          "type": "float",
          "required": false
        },
        "duration_seconds": {
          "type": "float",
          "default": 1.0,
          "required": false,
          "description": "Tween duration in seconds. Defaults to 1.0 (animated); <= 0 = immediate."
        }
      }
    }
  }
}
