{
  "system": "log",
  "description": "Read the editor's active log file. Only the active log file is scanned; older rotations (.1/.2/.3) are not included.",
  "queries": {
    "file_location": {
      "description": "Return absolute path of the active log file plus its existence and size in bytes",
      "params": {}
    },
    "read": {
      "description": "Return a filtered tail of the active log file (most recent N entries, in chronological order)",
      "params": {
        "min_severity": {
          "type": "string",
          "default": "Log",
          "options": [
            "VeryVerbose",
            "Verbose",
            "Log",
            "Display",
            "Warning",
            "Error",
            "Fatal"
          ],
          "required": false,
          "description": "Minimum log level to include"
        },
        "count": {
          "type": "int",
          "default": 200,
          "required": false,
          "description": "Max entries 1..5000 (clamped)"
        },
        "text_search": {
          "type": "string",
          "default": "",
          "required": false,
          "description": "Case-insensitive substring filter on the full log line"
        },
        "category": {
          "type": "string",
          "default": "",
          "required": false,
          "description": "Case-insensitive substring filter on the log category name"
        },
        "since_seconds": {
          "type": "int",
          "required": false,
          "description": "Only include entries whose [HH:MM:SS] timestamp is within the last N seconds (wall-clock). Lines with unparseable timestamps are skipped when this is set."
        }
      }
    }
  },
  "commands": {}
}
